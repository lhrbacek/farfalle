import { ActionIcon, Button, createStyles, Group, Text } from '@mantine/core';
import React from 'react';

export function OrderCard(ticket: TicketProps) {

  return (
    <div>
    </div>
  );
}

export default OrderCard;